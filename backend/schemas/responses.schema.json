{
    "type": "object",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
        "surveyId": {
            "type": "string",
            "title": "Unique ID of the survey"
        },
        "responses": {
            "type": "object",
            "title": "The responses to the survey",
            "additionalProperties": {
                "oneOf": [
                    { "type": ["string", "boolean", "number" ]}
                ]
            }
        }
    }
}